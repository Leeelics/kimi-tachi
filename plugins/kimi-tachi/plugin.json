{
  "name": "kimi-tachi",
  "version": "0.8.1",
  "description": "Kimi-tachi multi-agent orchestration plugin - run workflows and manage sessions",
  "tools": [
    {
      "name": "workflow",
      "description": "Run a multi-agent workflow for a task. Auto-detects team and supports category-based parallel orchestration.",
      "command": ["python3", "scripts/workflow.py"],
      "parameters": {
        "type": "object",
        "properties": {
          "task": {
            "type": "string",
            "description": "Task description to execute"
          },
          "workflow_type": {
            "type": "string",
            "default": "auto",
            "description": "Type of workflow to run (auto, or a pattern defined by the current team)"
          },
          "work_dir": {
            "type": "string",
            "default": ".",
            "description": "Working directory for the task"
          },
          "team": {
            "type": "string",
            "description": "Team ID to use (defaults to current team)"
          }
        },
        "required": ["task"]
      }
    },
    {
      "name": "list_agents",
      "description": "List all available agents for the current (or specified) team with their roles and icons",
      "command": ["python3", "scripts/list_agents.py"],
      "parameters": {
        "type": "object",
        "properties": {
          "team": {
            "type": "string",
            "description": "Team ID to list agents for (defaults to current team)"
          }
        },
        "required": []
      }
    },
    {
      "name": "get_agent_info",
      "description": "Get detailed information about a specific agent",
      "command": ["python3", "scripts/agent_info.py"],
      "parameters": {
        "type": "object",
        "properties": {
          "agent": {
            "type": "string",
            "description": "Agent name to get info for"
          },
          "team": {
            "type": "string",
            "description": "Team ID the agent belongs to (defaults to current team)"
          }
        },
        "required": ["agent"]
      }
    },
    {
      "name": "check_compatibility",
      "description": "Check kimi-cli version compatibility with kimi-tachi",
      "command": ["python3", "scripts/check_compatibility.py"],
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },

    {
      "name": "memory_search",
      "description": "Search code memory for relevant information (project-level search)",
      "command": ["python3", "scripts/memory_tool.py", "search"],
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Search query (e.g., 'authentication', 'User model', 'API endpoint')"
          },
          "limit": {
            "type": "integer",
            "default": 5,
            "description": "Maximum number of results"
          }
        },
        "required": ["query"]
      }
    },
    {
      "name": "memory_global_search",
      "description": "Search across all registered projects (cross-project knowledge)",
      "command": ["python3", "scripts/memory_tool.py", "global-search"],
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Search query for cross-project knowledge (e.g., 'JWT implementation', 'database pattern')"
          },
          "limit": {
            "type": "integer",
            "default": 5,
            "description": "Maximum number of results"
          }
        },
        "required": ["query"]
      }
    },
    {
      "name": "memory_recall_agent",
      "description": "Recall context and memories for a specific agent before starting work",
      "command": ["python3", "scripts/memory_tool.py", "recall"],
      "parameters": {
        "type": "object",
        "properties": {
          "agent": {
            "type": "string",
            "description": "Agent to recall context for"
          },
          "task": {
            "type": "string",
            "description": "Current task description to find relevant memories"
          }
        },
        "required": ["agent"]
      }
    },
    {
      "name": "memory_store_decision",
      "description": "Store an important decision or finding to memory",
      "command": ["python3", "scripts/memory_tool.py", "store"],
      "parameters": {
        "type": "object",
        "properties": {
          "content": {
            "type": "string",
            "description": "Decision or finding to store (e.g., 'Use Redis for session storage', 'API rate limit: 100 req/min')"
          },
          "category": {
            "type": "string",
            "enum": ["decision", "finding", "pattern", "architecture"],
            "default": "finding",
            "description": "Category of the memory"
          },
          "tags": {
            "type": "array",
            "items": {"type": "string"},
            "default": [],
            "description": "Tags for the memory (e.g., ['auth', 'redis', 'performance'])"
          }
        },
        "required": ["content"]
      }
    }
  ]
}
